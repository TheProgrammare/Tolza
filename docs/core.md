# Regroup all essentials libs for the user comfort
Libs imported as native root namespace to avoid boilerplate for essentials functions

Input Outputs `io`
- `out::format()`
- `out()` or `print()`
- `out::line()`
- `out::error()`
- `out::warning()`
- `out::info()`
- `in::integral()`
- `in::decimal()`
- `in::textual()`

