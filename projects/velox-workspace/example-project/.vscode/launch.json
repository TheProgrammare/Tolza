{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Velox Release",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/build/release",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Velox: Build",
        }
    ]
}